

HSV colorspace
hue -> this channel will be mostly used in the programm, for storing information about the color of the image and of all the different pixels in the image.
I have to specify a range or "region" of colors (as two values) which the programm has to recognize and give me all the pixels that correspond to that region.
saturation
value